# Gradient Descent

Gradient Descent is an optimization algorithm used to train machine learning models.

The goal is to minimize error by gradually adjusting model parameters.

Key idea:
- Calculate error
- Compute gradient
- Update weights
- Repeat until error reduces
