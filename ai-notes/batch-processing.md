# Batch Processing

Batch processing is when data is processed in groups (batches) instead of one by one.

It is commonly used in AI training to improve efficiency and speed.

Example:
- Training models on batches of images
