# Normalization vs Standardization

Normalization rescales values to a fixed range, often between 0 and 1.

Standardization transforms values so they have a mean of 0 and a standard deviation of 1.

Normalization is useful when features have very different ranges.

Standardization is commonly used before training many machine learning models.