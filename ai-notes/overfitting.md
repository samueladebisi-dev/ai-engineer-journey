# Overfitting in AI

Overfitting happens when an AI model learns training data too well and performs poorly on new data.

Signs of overfitting:
- High training accuracy
- Poor real-world performance

Using more data and regularization can help reduce overfitting.