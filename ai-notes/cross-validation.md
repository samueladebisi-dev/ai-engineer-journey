# Cross Validation

Cross validation is a technique for evaluating machine learning models.

Instead of splitting the data once, the dataset is divided into multiple folds.

The model trains and tests on different folds to produce a more reliable performance estimate.

Benefits:
- Better evaluation
- Reduces overfitting risk
- Makes better use of available data
