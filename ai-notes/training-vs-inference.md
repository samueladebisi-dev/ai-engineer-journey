# Training vs Inference

Training is when an AI model learns from data.

Inference is when a trained model is used to make predictions.

Example:
- Training: teaching the model
- Inference: using the model
