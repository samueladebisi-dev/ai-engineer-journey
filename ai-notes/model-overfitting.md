# Model Overfitting

Overfitting happens when a machine learning model learns the training data too well, including its noise.

Signs of overfitting:

- High training accuracy
- Poor test accuracy

Ways to reduce overfitting:

- More training data
- Cross validation
- Regularization
- Simpler models